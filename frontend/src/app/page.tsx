'use client';

import { LibraryScreen } from '@/components/Library/LibraryScreen';

/** The home screen is the meeting library; recording lives at /record. */
export default function Home() {
  return <LibraryScreen />;
}
